# Project Memories

Each file in this directory stores project-specific state.

- `*.md` keeps automatic breadcrumbs
- `*.summary.md` keeps the generated resume snapshot
- `*.notes.md` keeps manual notes that are never overwritten automatically
